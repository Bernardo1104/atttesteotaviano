package com.example;

public class DiscountCalculator {

    public double calculateDiscount(double amount) {

        if (amount < 100) {
            return amount;
        } else if (amount < 300) {
            return amount * 0.9;
        } else {
            return amount * 0.8;
        }
    }
}
